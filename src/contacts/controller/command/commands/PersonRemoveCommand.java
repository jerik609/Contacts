package contacts.controller.command.commands;

import contacts.controller.Contacts;
import contacts.controller.command.Command;

public class PersonRemoveCommand implements Command {

    private Contacts contacts;

    @Override
    public void execute() {

    }
}
